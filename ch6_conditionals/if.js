let age = 17;

if (age >= 18) {
    console.log("You are an adult.");
    } 

    if (age < 18) {
    console.log("You are Not an adult.");
    }  // why 'Your are an asult are not showing? becuase this condition (if) is protecting, it's checking first if the age is 18 or less.
    // (if) are how computers make decisions, what line it will show the users.